import 'package:flutter/material.dart';

// 3️⃣ Exercices et évaluations
class ExercicesEvaluationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Exercices & Évaluations")),
      body: Center(child: Text("Création et suivi des exercices")),
    );
  }
}