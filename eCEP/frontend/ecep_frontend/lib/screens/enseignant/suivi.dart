import 'package:flutter/material.dart';

// 5️⃣ Suivi des performances
class SuiviPerformancesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Suivi des Performances")),
      body: Center(child: Text("Analyse des résultats des élèves")),
    );
  }
}