import 'package:flutter/material.dart';

// 3️⃣ Exercices et évaluations
class ExamensPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Examens")),
      body: Center(child: Text("vos exzamens  apparaitrons ici")),
    );
  }
}
